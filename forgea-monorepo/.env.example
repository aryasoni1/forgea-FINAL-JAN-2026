# Forgea Environment Variables (canonical root example)
# This file is safe to commit. Populate a local .env or .env.local privately.

# Required (fail-closed today)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/forgea_db"

# Optional sinks (no-op if unset)
FORGEA_AUDIT_SINK_URL="https://audit.example.com/ingest"
FORGEA_SECURITY_ALERT_SINK_URL="https://security.example.com/alerts"

# GitHub OAuth + webhook (Forgea Labs)
GITHUB_ID="your-github-oauth-client-id"
GITHUB_SECRET="your-github-oauth-client-secret"
GITHUB_WEBHOOK_SECRET="your-github-webhook-secret"

# Runtime-provided (typically set by Next.js / Node.js)
NODE_ENV="development"
