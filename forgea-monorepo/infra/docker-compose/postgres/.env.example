# Non-secret defaults for local development
POSTGRES_DB=forgea_dev
POSTGRES_USER=forgea
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Example only. Do not treat as authoritative for secrets.
DATABASE_URL=postgresql://forgea:change-me@localhost:5432/forgea_dev?sslmode=require
